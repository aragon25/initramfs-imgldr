# Local filesystem mounting      -*- shell-script -*-

#
# This script overrides local_mount_root() in /scripts/local
# and mounts root as a read-only filesystem with a temporary (rw)
# overlay image filesystem.
#

. /scripts/local

# Load imgldr functions
if [ -f /scripts/imgldr_functions ]; then
  . /scripts/imgldr_functions
else
  echo "... ERROR: IMGLDR FAILED TO LOAD FUNCTIONS ..."
  if command -v setsid >/dev/null 2>&1; then
    read -r console rest </proc/consoles
    if [ "${console}" = "tty0" ]; then
      console="tty1"
    fi
    setsid sh -c "exec sh -i <>/dev/${console} 1>&0 2>&1"
  else
    sh -i </dev/console >/dev/console 2>&1
  fi
  echo "Rebooting in 5 seconds..."
  sleep 5
  reboot -f
fi

local_mount_root()
{
  local_top
  if [ -z "${ROOT}" ]; then
    imgldr_panic_pretty "     !!! KERNEL PANIC !!!" "  No root device specified." " Boot arguments must include" "      a root= parameter."
  fi
  local_device_setup "${ROOT}" "root file system"
  ROOT="${DEV}"

  # Get the root filesystem type if not set
  if [ -z "${ROOTFSTYPE}" ] || [ "${ROOTFSTYPE}" = auto ]; then
    FSTYPE=$(get_fstype "${ROOT_DEV}")
  else
    FSTYPE=${ROOTFSTYPE}
  fi

  local_premount

  # Check needed modules
  if ! modprobe squashfs; then
    imgldr_panic_pretty "     !!! KERNEL PANIC !!!" "    Failed to load squashfs" "        kernel module"
  fi
  if ! modprobe ${FSTYPE}; then
    imgldr_panic_pretty "     !!! KERNEL PANIC !!!" "    Failed to load ${FSTYPE}" "        kernel module"
  fi
  checkfs ${ROOT} root "${FSTYPE}"
  mkdir -p /conf
  touch /conf/param.conf
  echo "ROOT_FSTYPE=$FSTYPE" >> "/conf/param.conf"

  # Reload variables from param.conf if they were changed in premount script
  imgldr_reload_vars

  # Mount the root and boot devices and system.sqfs
  imgldr_mount_fs

  # Run premount script if it exists
  local premount_script="/lib/imgldr/premount"
  if [ -f "$premount_script" ]; then
    /bin/sh -c "$premount_script"
    if [ $? -ne 0 ]; then
      echo "... WARNING: IMGLDR PREMOUNT SCRIPT FAILED ..."
    fi
    imgldr_reload_vars
  fi

  # check if the filesystems are mounted correctly
  imgldr_check_fs

  # Mount the final root filesystem at ${rootmnt}
  local mount_script="/lib/imgldr/mount"
  if [ -f "$mount_script" ]; then
    /bin/sh -c "$mount_script"
    if [ $? -ne 0 ]; then
      imgldr_panic_pretty "     !!! KERNEL PANIC !!!" "imgldr mount script failed!"
    fi
    imgldr_reload_vars
  else
    imgldr_panic_pretty "     !!! KERNEL PANIC !!!" "    Failed to find IMGLDR" "        mount script!" "     Cannot mount rootfs!"
  fi

  # Run postmount script if it exists
  local postmount_script="/lib/imgldr/postmount"
  if [ -f "$postmount_script" ]; then
    /bin/sh -c "$postmount_script"
    if [ $? -ne 0 ]; then
      echo "... WARNING: IMGLDR POSTMOUNT SCRIPT FAILED ..."
    fi
    imgldr_reload_vars
  fi

  # check if init script is found in the new root filesystem
  [ -z "${init}" ] && init="/sbin/init"
  if ! [ -f "${rootmnt}${init}" ]; then
    imgldr_panic_pretty "     !!! KERNEL PANIC !!!" "   init script not found in" "      root filesystem!" "  Cannot continue booting!"
  fi

  unset ROOT_MOUNT
  unset BOOT_MOUNT
  unset SQFS_MOUNT
}
